rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY TEST RULES - ALLOWS ALL AUTHENTICATED USERS
    // Use this to verify data loads, then switch to secure rules
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
